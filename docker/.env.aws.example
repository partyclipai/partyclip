# AWS ECS Fargate deployment environment
# Copy to .env.aws and fill in values before deploying
#
# Secrets (DATABASE_URL, BETTER_AUTH_SECRET, ANTHROPIC_API_KEY, OPENAI_API_KEY,
# GITHUB_TOKEN) are injected via AWS Secrets Manager — do NOT set them here.

# Deployment mode
PARTYCLIP_DEPLOYMENT_MODE=authenticated
PARTYCLIP_DEPLOYMENT_EXPOSURE=public
PARTYCLIP_PUBLIC_URL=https://partyclip.example.com

# Server
HOST=0.0.0.0
PORT=3100
NODE_ENV=production
SERVE_UI=true

# Paperclip paths
PARTYCLIP_HOME=/partyclip
PARTYCLIP_INSTANCE_ID=default
PARTYCLIP_CONFIG=/partyclip/instances/default/config.json

# Auto-apply migrations on startup
PARTYCLIP_MIGRATION_AUTO_APPLY=true

# Enable heartbeat scheduler for remote agents
HEARTBEAT_SCHEDULER_ENABLED=true

# Post-deploy hardening (uncomment after first user signs up)
# PARTYCLIP_AUTH_DISABLE_SIGN_UP=true
